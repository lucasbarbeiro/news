import './bootstrap';

import Vue from 'vue';
import VueToastify from 'vue-toastify';

export default {
    name: 'App',
};

Vue.use(VueToastify)

/*
    const app = Vue({});
    app.use(VueToastify);
    app.mount('#App')
*/